{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.md ]] && npx --yes prettier --write \"$f\" 2>/dev/null || true; }",
            "statusMessage": "Formatting markdown..."
          }
        ]
      }
    ]
  }
}
